---
name: Build Image
on: [push]
jobs:
  build:
    name: Build Image
    container: fedora:latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Create Image
        run: |
          sudo dnf install mkosi pacman zypper apt
          ls -la $GITHUB_WORKSPACE/
          sudo mkosi -C $GITHUB_WORKSPACE/ --force build
          tree $GITHUB_WORKSPACE/
